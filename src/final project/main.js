// main.js
import {
  toggleForms,
  removeToken,
  goHome,
  verifyLogin,
} from "./login-signup page/script";

// Attach functions to window object for global access
window.toggleForms = toggleForms;
//window.removeToken = removeToken;
window.goHome = goHome;
window.verifyLogin = verifyLogin;
